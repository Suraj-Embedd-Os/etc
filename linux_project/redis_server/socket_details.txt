
// Server and client model using TCP

server:
fp=socket()
bind(fd,address)
listen(fd)

while true:
	conn_fd=accept(fd)
	do_something_with(conn_fd)
	close(conn_fd)
	
client:
fp=socket()
connect(fd,address)
do_something_with(fd)
close(conn_fd)